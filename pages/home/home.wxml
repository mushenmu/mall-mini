<view class="page">
  <!-- 轮播图 -->
  <swiper class="swiper" indicator-dots autoplay circular interval="4000" duration="500"
    wx:if="{{swiper.length}}">
    <swiper-item wx:for="{{swiper}}" wx:key="index">
      <image class="swiper-img" src="{{item.image}}" mode="aspectFill" />
    </swiper-item>
  </swiper>

  <!-- 热门商品 -->
  <view class="section">
    <view class="section-title">
      <text>热门推荐</text>
      <text class="section-more" bindtap="goSearch">搜索商品 ›</text>
    </view>
    <view class="goods-grid">
      <view class="goods-card" wx:for="{{hotGoods}}" wx:key="id" bindtap="goGoods" data-id="{{item.id}}">
        <image class="thumb" src="{{item.image}}" mode="aspectFill" lazy-load />
        <view class="info">
          <view class="title">{{item.title}}</view>
          <view class="price-row">
            <view class="price"><text class="symbol">¥</text>{{item.price}}</view>
            <view class="sales">已售{{item.sales}}</view>
          </view>
        </view>
      </view>
    </view>
  </view>

  <view class="empty" wx:if="{{!loading && !error && !hotGoods.length}}">暂无商品</view>
  <view class="error-state" wx:if="{{error}}"><text>{{error}}</text><text class="retry" bindtap="retry">重新加载</text></view>
</view>
